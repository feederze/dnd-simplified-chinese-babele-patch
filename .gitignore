# dependencies
node_modules/

# build outputs (produced by `npm run build` / CI)
dnd-simplified-chinese-babele-patch/scripts/
tools/*.mjs

# translation content (fetched in CI)
dnd-simplified-chinese-babele-patch/translation/*

# misc
.gitattributes
*.bak
*.json.json
